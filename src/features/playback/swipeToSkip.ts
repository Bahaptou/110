/**
 * How far a horizontal drag must travel before it counts as a skip. Shared by the mini player and
 * the full player's cover so both react at the same point.
 */
export const SKIP_DISTANCE = 60;
